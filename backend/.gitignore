# ═══════════════════════════════════════════════════════════════════════════
# YOU+ Backend (Cloudflare Worker) .gitignore
# ═══════════════════════════════════════════════════════════════════════════
# Note: Root .gitignore handles common patterns (node_modules, .env, AI agents, etc.)

# Trigger.dev
.trigger/

# Cloudflare Wrangler
.wrangler/
.mf/
.miniflare/
.dev.vars

# Environment (also in root)
.env*
!.env.example
!.env.template
